<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Miniblog avec pagination zurb" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <div class="top-bar">
  <div class="top-bar-left">
    <ul class="dropdown menu" data-dropdown-menu>
      <li class="menu-text"><a href="/">Miniblog avec pagination</a></li>
    <li class="divider"></li>
    <li class="toggle-topbar menu-icon"><a href="/"><span>Accueil</span></a></li>
  </ul>
    </div>
</div>

    <%= yield %>

  </body>
</html>
